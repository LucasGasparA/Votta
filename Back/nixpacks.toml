[phases.setup]
nixPkgs = ["python312", "python312Packages.pip", "gcc"]

[phases.install]
cmds = [
  "npm ci",
  "pip3 install --user -r requirements.txt"
]

[phases.build]
cmds = [
  "npx prisma generate",
  "npm run build"
]
